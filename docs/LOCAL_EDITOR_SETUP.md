# Настройка локального редактора статей

## 🎯 Цель

Локальный редактор статей (`admin.html`) позволяет писать и редактировать статьи локально, а они автоматически загружаются в GitHub и публикуются на прод.

## 📋 Структура работы

```
Локально (source/)
├── admin.html              # Редактор (только локально!)
├── js/admin.js            # Логика редактора (только локально!)
├── js/github-api.js       # GitHub API клиент (только локально!)
├── js/markdown-converter-browser.js  # Конвертер (только локально!)
└── css/admin.css          # Стили редактора (только локально!)

GitHub репозиторий
├── blog/content/          # Markdown файлы статей (исходники)
├── blog/articles.json     # Метаданные статей
├── blog/posts/            # HTML файлы (генерируются автоматически)
└── blog/images/           # Изображения для статей

GitHub Actions
└── .github/workflows/build-articles.yml  # Автоматическая сборка HTML из markdown
```

## 🔄 Процесс работы

### 1. Локальная работа с редактором

1. Откройте `admin.html` в браузере (локально, из папки `source/`)
2. Введите GitHub Personal Access Token
3. Создайте или отредактируйте статью
4. Нажмите "💾 Сохранить в GitHub"

### 2. Что происходит при сохранении

При сохранении статьи через редактор:

1. **Markdown файл** сохраняется в `blog/content/<article-id>.md` в GitHub
2. **Метаданные** обновляются в `blog/articles.json` в GitHub
3. **Изображения** загружаются в `blog/images/<article-id>/` в GitHub
4. **GitHub Actions** автоматически запускается при изменении файлов

### 3. Автоматическая сборка

GitHub Actions workflow (`build-articles.yml`) автоматически:

1. Запускается при изменении:
   - `blog/content/**` (markdown файлы)
   - `blog/articles.json` (метаданные)
   - Скриптов сборки

2. Выполняет:
   - Читает markdown из `blog/content/`
   - Конвертирует в HTML через `scripts/build-article.js`
   - Сохраняет HTML в `blog/posts/`
   - Коммитит и пушит изменения

3. Результат:
   - HTML статьи появляется в `blog/posts/`
   - Сайт обновляется через GitHub Pages

## 🛡️ Защита от попадания редактора в прод

### Скрипт деплоя

Скрипт `tools/scripts/deploy-portfolio.ps1` автоматически исключает редактор из продакшена:

```powershell
# Исключаем локальный редактор статей и связанные файлы из продакшена
robocopy $sourcePath $deployPath *.* /E /MIR /XD '.git' '.github' 'node_modules' /XF \
    'start-site.cmd' \
    'admin.html' \
    'js/admin.js' \
    'js/github-api.js' \
    'js/markdown-converter-browser.js' \
    'css/admin.css' \
    'docs/ADMIN_EDITOR.md'
```

### Проверка

Убедитесь, что в папке `github-pages/` НЕТ:
- ❌ `admin.html`
- ❌ `js/admin.js`
- ❌ `js/github-api.js`
- ❌ `js/markdown-converter-browser.js`
- ❌ `css/admin.css`

## ⚙️ Настройка GitHub Personal Access Token

### Создание токена

1. Перейдите на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token (classic)"
3. Установите права:
   - ✅ `repo` (полный доступ к репозиторию)
   - ✅ `workflow` (для GitHub Actions)
4. Скопируйте токен (он показывается только один раз!)

### Использование в редакторе

1. Откройте `admin.html` в браузере
2. Введите токен в поле "GitHub Personal Access Token"
3. Нажмите "Сохранить"
4. Токен сохранится в localStorage браузера

## 📝 Рабочий процесс

### Создание новой статьи

1. Откройте `admin.html` локально
2. Нажмите "+ Новая"
3. Заполните метаданные:
   - Заголовок
   - Дата публикации
   - Теги
   - Краткое описание
   - Время чтения
   - Статус (draft/published)
   - Иконка
4. Напишите статью в редакторе (WYSIWYG или Markdown)
5. Нажмите "💾 Сохранить в GitHub"
6. Статья сохранится в GitHub, GitHub Actions соберёт HTML автоматически

### Редактирование статьи

1. Откройте `admin.html` локально
2. Выберите статью из списка
3. Внесите изменения
4. Нажмите "💾 Сохранить в GitHub"
5. GitHub Actions обновит HTML автоматически

### Загрузка изображений

1. Откройте статью в редакторе
2. Перетащите изображение в область "📷 Перетащите изображение сюда"
3. Изображение автоматически загрузится в `blog/images/<article-id>/`
4. Ссылка на изображение вставится в статью
5. Статья автоматически сохранится

## 🔍 Проверка работы

### Локально

1. Откройте `admin.html` в браузере
2. Проверьте, что токен установлен
3. Создайте тестовую статью
4. Сохраните в GitHub
5. Проверьте в GitHub репозитории, что файлы появились

### GitHub Actions

1. Перейдите в GitHub репозиторий
2. Откройте вкладку "Actions"
3. Проверьте, что workflow "Build Articles" запустился
4. Проверьте, что он успешно завершился
5. Проверьте, что HTML файл появился в `blog/posts/`

### Прод

1. Подождите несколько минут после сохранения
2. Проверьте сайт: `https://yourmoontg.github.io/blog.html`
3. Убедитесь, что статья появилась в списке

## 🐛 Устранение проблем

### "Токен не установлен"

- Убедитесь, что ввели токен и нажали "Сохранить"
- Проверьте, что токен имеет права `repo` и `workflow`

### "Ошибка сохранения"

- Проверьте, что токен действителен
- Проверьте права токена
- Проверьте консоль браузера для деталей ошибки

### "GitHub Actions не запускается"

- Проверьте, что workflow файл существует: `.github/workflows/build-articles.yml`
- Проверьте, что файлы изменились в правильных путях:
  - `blog/content/**`
  - `blog/articles.json`
- Проверьте логи GitHub Actions

### "HTML не генерируется"

- Проверьте логи GitHub Actions
- Убедитесь, что markdown файл существует в `blog/content/`
- Убедитесь, что `articles.json` содержит правильные метаданные
- Проверьте, что скрипт `build-article.js` работает локально

## 📚 Дополнительная информация

- [Инструкция по веб-редактору](ADMIN_EDITOR.md)
- [Структура проекта](STRUCTURE.md)
- [План работы](plans/WORK_PLAN.md)

## ⚠️ Важно

1. **Редактор работает только локально** - не открывайте `admin.html` на проде
2. **Токен хранится в localStorage** - не коммитьте токен в репозиторий
3. **Все изменения идут через GitHub** - локальные изменения не влияют на прод напрямую
4. **GitHub Actions собирает HTML** - не нужно собирать статьи вручную


