# Блог

Структура блога использует конструктор статей: метаданные в JSON, контент в markdown файлах, автоматическая сборка в HTML.

## 🚀 Быстрый старт

**Рекомендуемый способ:** Используйте веб-редактор (`admin.html`) для написания статей прямо из браузера.

**Альтернативный способ:** Используйте CLI утилиты для создания и сборки статей.

## Структура

- `articles.json` - метаданные всех статей
- `content/` - markdown файлы статей (исходники)
- `posts/` - HTML файлы статей (собираются автоматически)
- `images/` - изображения для статей

## Рабочий процесс

### Вариант 1: Веб-редактор (рекомендуется) 🌐

1. Откройте `admin.html` в браузере
2. Введите GitHub Personal Access Token (если ещё не вводили)
3. Нажмите **"+ Новая"** для создания статьи
4. Заполните метаданные и напишите статью
5. Нажмите **"💾 Сохранить в GitHub"**
6. GitHub Actions автоматически соберёт HTML

**Преимущества:**
- Удобный WYSIWYG редактор
- Автоматическое сохранение в GitHub
- Загрузка изображений через drag & drop
- Предпросмотр в реальном времени

Подробная инструкция: [docs/ADMIN_EDITOR.md](../docs/ADMIN_EDITOR.md)

---

### Вариант 2: CLI утилиты 💻

#### 1. Создание новой статьи

```bash
node scripts/add-article.js
```

Скрипт создаст:
- Запись в `articles.json` с метаданными
- Markdown файл в `blog/content/<article-id>.md`

### 2. Написание статьи

Отредактируйте markdown файл в `blog/content/<article-id>.md`. 

**Формат markdown поддерживает:**

- **Обычный текст** - просто пишите
- **Заголовки** - `# H1`, `## H2`, `### H3`
- **Изображения:**
  - `![Описание](путь/к/изображению.webp)` - стандартный markdown
  - `[IMAGE:путь/к/изображению.webp]` - простой формат
  - `[IMAGE:путь/к/изображению.webp|Описание]` - с описанием
- **Сворачиваемые секции:**
  ```
  >>> Заголовок секции
  Контент секции, который можно свернуть
  <<<
  ```
- **Код:**
  `\`inline код\`` или блоки с ` ```язык `
- **Списки** - `- пункт` или `1. пункт`
- **Цитаты** - `> текст`
- **Ссылки** - `[текст](url)`

### 3. Добавление изображений

Поместите изображения в `blog/images/<article-id>/` и используйте в markdown:
- `[IMAGE:имя-файла.webp]` - если файл в папке статьи
- `[IMAGE:путь/к/файлу.webp]` - если файл в общей папке images

#### 4. Сборка статьи

```bash
# Собрать одну статью
node scripts/build-article.js <article-id>

# Собрать все статьи
node scripts/build-article.js --all
```

Скрипт:
- Читает markdown из `content/<article-id>.md`
- Конвертирует в HTML
- Вставляет в шаблон
- Сохраняет в `posts/<date>-<article-id>.html`

#### 5. Публикация

Измените статус в `articles.json` на `"published"` и соберите статью.

---

## Автоматическая сборка

При использовании веб-редактора или коммите изменений в `blog/content/`:
- GitHub Actions автоматически запускает сборку
- Markdown конвертируется в HTML
- HTML файлы сохраняются в `blog/posts/`
- Сайт обновляется через GitHub Pages

## Использование утилит

- `scripts/add-article.js` - создание новой статьи (метаданные + markdown шаблон)
- `scripts/build-article.js` - сборка статьи из markdown в HTML
- `scripts/validate-blog.js` - валидация структуры блога

## Пример markdown статьи

```markdown
# Заголовок статьи

Введение в статью.

>>> Секция с деталями
Подробная информация, которую можно свернуть.

- Пункт 1
- Пункт 2
<<<

## Основной раздел

Текст статьи с **жирным** и *курсивом*.

![Описание](image.webp)

\`\`\`javascript
function example() {
    return "Hello!";
}
\`\`\`
```

